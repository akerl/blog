language: ruby
cache: bundler
sudo: false
bundler_args: --without ""
rvm:
- 2.2.0
notifications:
  email: false
  irc:
    template:
    - "%{repository}/%{branch}/%{build_number}: %{message} -- %{build_url}"
    channels:
    - irc.oftc.net#akerl
